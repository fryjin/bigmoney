# Codex Task：Phase 2.0A Payment / Liquidation / Bankruptcy

## 任务目标

在现有八节点技术切片中建立统一的强制付款、地产清算、破产淘汰和胜者判定基础。该阶段只实现规则基础设施，不扩展36格地图。

## 冻结规则

- 强制付款可能触发资产清算。
- 只允许清算地产。
- 地产清算价值为购买价的50%。
- 股票和合作项目不可清算。
- 主动购买地产或升级不得主动进入负现金或破产。
- 无法通过现金与地产清算完成强制付款时，玩家破产并被淘汰。
- 最后一名未破产玩家获胜。
- 货币内部单位 `1 = 10万元`，显示层使用“万/亿”。

## 明确不做

- 不实现监狱。
- 不实现合作项目。
- 不实现完整卡牌响应窗口。
- 不扩展36格地图。
- 不替换正式美术。
- 不改变现有经济数值。

## 架构要求

### Game Core

在 `packages/game-core` 内完成：

- 统一付款请求模型；
- 可支付、需清算、无法支付三类结果；
- 地产可清算列表与50%价值计算；
- 单项或多项地产清算命令；
- 付款完成；
- 破产淘汰；
- 跳过已破产玩家；
- 最后存活玩家胜者判定；
- 全部规则单元测试。

### Game Flow

在 `packages/game-flow` 内完成：

- `payment`；
- `liquidation`；
- `bankruptcy`；
- `finished`；
- 中断前状态恢复；
- 重复提交保护；
- 流程测试。

不得在 XState action 中重新计算租金、清算价值或胜负条件。

### Web UI

在 `apps/web` 内完成：

- 强制付款摘要；
- 可清算地产列表；
- 已选择清算金额；
- 剩余缺口；
- 确认清算；
- 破产结果；
- 胜者结果；
- 同设备交接期间继续隐藏私有信息。

### Persistence

- 仅在稳定节点保存；
- 新增字段必须升级存档 Schema；
- 必须提供旧版存档迁移或明确隔离策略；
- 不恢复到清算选择或动画中间。

## 必须测试

1. 现金足够，直接付款。
2. 现金不足，清算一项地产后付款。
3. 现金不足，清算多项地产后付款。
4. 清算全部地产仍不足，玩家破产。
5. 股票不出现在清算列表。
6. 主动购买余额不足时拒绝，不进入清算。
7. 破产玩家后续回合自动跳过。
8. 最后存活玩家进入 `finished`。
9. 重复点击清算确认不会重复加钱或重复付款。
10. 刷新只回到最近稳定节点。

## 完成标准

```bash
npm run check
```

必须通过，并在最终汇报中提供：

- 修改文件列表；
- 新增领域事件和命令；
- XState状态变化；
- 测试数量与结果；
- 尚未覆盖的边界；
- 建议提交信息。
